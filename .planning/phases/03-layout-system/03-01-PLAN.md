---
phase: 03-layout-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/components/Header.tsx
  - apps/web/src/components/Footer.tsx
autonomous: true

# Goal-backward verification
must_haves:
  truths:
    - "Header displays logo/name on left side"
    - "Header displays Inicio navigation link on right side"
    - "Footer component exists and can be hidden/shown"
  artifacts:
    - path: "apps/web/src/components/Header.tsx"
      provides: "Header with logo/name left, nav right"
      min_lines: 15
    - path: "apps/web/src/components/Footer.tsx"
      provides: "Footer component ready for scroll-based reveal"
      min_lines: 10
  key_links:
    - from: "Header.tsx"
      to: "Tailwind classes"
      via: "flex justify-between layout"
      pattern: "flex.*justify-between"
---

<objective>
Create Header and Footer components as foundational layout building blocks.

Purpose: These components are prerequisites for the Layout orchestration. Header shows immediately with branding and nav. Footer will be revealed on scroll.
Output: Two reusable React components with Tailwind v4 styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

# Prior work establishing patterns:
@.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md

# Current source files:
@apps/web/src/App.tsx
@apps/web/src/styles/main.css
@apps/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion for animations</name>
  <files>apps/web/package.json</files>
  <action>
    Install framer-motion package for future animation support:
    ```bash
    cd apps/web && bun add framer-motion
    ```

    This prepares for Plan 03-02 which will use motion components for progressive loading transitions.

    Version should be ^12.x (latest stable as per STACK.md research).
  </action>
  <verify>
    - `grep "framer-motion" apps/web/package.json` shows dependency
    - `bun install` succeeds at root
  </verify>
  <done>framer-motion added to apps/web dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create Header component</name>
  <files>apps/web/src/components/Header.tsx</files>
  <action>
    Create Header component with:

    **Left side:**
    - Logo/photo placeholder (can be image or styled initials "AC")
    - Name "Alvaro Cortes"

    **Right side:**
    - Navigation with single "Inicio" link

    **Styling (Tailwind v4):**
    - Fixed position at top (will render immediately)
    - Dark background matching current App.tsx (neutral-900)
    - White text
    - Flex layout with justify-between for left/right positioning
    - Proper padding and height
    - Typography from PROJECT.md: Proxima Nova fallback chain (use sans-serif for now)

    **Structure:**
    ```tsx
    export function Header() {
      return (
        <header className="...">
          <div className="...">
            {/* Logo/Name left */}
          </div>
          <nav className="...">
            {/* Inicio link right */}
          </nav>
        </header>
      )
    }
    ```

    The Header should NOT include any animation logic - that's handled by Layout in Plan 03-02.
  </action>
  <verify>
    - File exists at apps/web/src/components/Header.tsx
    - `bun run type-check` passes in apps/web
    - Component exports Header function
  </verify>
  <done>Header component created with logo/name left, Inicio nav right</done>
</task>

<task type="auto">
  <name>Task 3: Create Footer component</name>
  <files>apps/web/src/components/Footer.tsx</files>
  <action>
    Create Footer component with:

    **Content:**
    - Simple footer with copyright text
    - "Alvaro Cortes © 2026"
    - Can add social links placeholder (not functional yet)

    **Styling (Tailwind v4):**
    - Dark background matching Header (neutral-900 or similar)
    - White/gray text
    - Centered content
    - Proper padding

    **Structure:**
    ```tsx
    export function Footer() {
      return (
        <footer className="...">
          {/* Copyright and optional links */}
        </footer>
      )
    }
    ```

    The Footer should NOT include visibility/scroll logic - that's handled by Layout in Plan 03-02.
    Keep it simple - just the visual structure.
  </action>
  <verify>
    - File exists at apps/web/src/components/Footer.tsx
    - `bun run type-check` passes in apps/web
    - Component exports Footer function
  </verify>
  <done>Footer component created with copyright text, ready for scroll reveal</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install` succeeds at repo root
- [ ] `bun run type-check` passes in apps/web
- [ ] `bun run build` succeeds
- [ ] Header.tsx exists with proper structure
- [ ] Footer.tsx exists with proper structure
- [ ] framer-motion is in package.json dependencies
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Header component renders logo/name left, nav right
- Footer component ready for Layout integration
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-system/03-01-SUMMARY.md`
</output>
