---
phase: 08-projects-bento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/data/projects.ts
  - apps/web/src/components/ProjectsBento.tsx
  - apps/web/src/components/PortfolioPage.tsx
autonomous: false

must_haves:
  truths:
    - "Projects section displays bento grid layout with varying card sizes"
    - "Project cards show name, description, and tech badges"
    - "3-4 placeholder projects are visible"
    - "Hover effect lifts and slightly enlarges cards"
  artifacts:
    - path: "apps/web/src/data/projects.ts"
      provides: "Project data with 3-4 placeholder entries"
      min_lines: 40
    - path: "apps/web/src/components/ProjectsBento.tsx"
      provides: "Bento grid with project cards and hover effects"
      min_lines: 60
  key_links:
    - from: "PortfolioPage.tsx"
      to: "ProjectsBento"
      via: "component import"
      pattern: "import.*ProjectsBento"
    - from: "ProjectsBento.tsx"
      to: "projects.ts"
      via: "data import"
      pattern: "import.*projects"
---

<objective>
Create Projects section with bento grid layout and interactive project cards.

Purpose: Showcase projects in visually interesting bento grid with hover effects and tech badges.
Output: Working ProjectsBento component integrated into PortfolioPage with placeholder projects.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/components/PortfolioPage.tsx
@apps/web/src/components/TechStacks.tsx
@apps/web/src/data/techStacks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projects data file</name>
  <files>apps/web/src/data/projects.ts</files>
  <action>
    Create apps/web/src/data/projects.ts with:

    1. Define Project type:
       - id: string
       - name: string
       - description: string
       - techStack: string[] (technology names)
       - imageUrl?: string (optional placeholder image)
       - liveUrl?: string
       - repoUrl?: string
       - featured: boolean (determines card size in bento)
       - details: string (longer description for modal)

    2. Export PROJECTS array with 4 placeholder projects:

    **Project 1 (featured: true):**
    - name: "Portfolio Personal"
    - description: "Sitio web personal construido con React, TypeScript y Tailwind"
    - techStack: ["React", "TypeScript", "Tailwind", "Vite"]
    - details: "Mi portafolio personal showcasing proyectos y experiencia..."

    **Project 2 (featured: false):**
    - name: "Task Manager API"
    - description: "REST API para gestión de tareas con autenticación JWT"
    - techStack: ["Node.js", "Express", "PostgreSQL", "Prisma"]

    **Project 3 (featured: true):**
    - name: "E-commerce Dashboard"
    - description: "Panel de administración para tienda online"
    - techStack: ["React", "TypeScript", "TanStack Query", "Recharts"]

    **Project 4 (featured: false):**
    - name: "CLI Tool"
    - description: "Herramienta de línea de comandos para automatización"
    - techStack: ["Node.js", "Commander", "Chalk"]

    Note: Use descriptive Spanish text. Featured projects get larger cards.
  </action>
  <verify>bun run type-check passes</verify>
  <done>Projects data file created with 4 typed entries</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectsBento component with hover effects</name>
  <files>apps/web/src/components/ProjectsBento.tsx</files>
  <action>
    Create ProjectsBento.tsx component:

    1. Import PROJECTS from data/projects
    2. Create bento grid layout:
       - CSS Grid with auto-placement
       - Featured projects: col-span-2 on md+ screens
       - Regular projects: col-span-1
       - Responsive: single column mobile, 2-3 cols desktop

    3. ProjectCard component:
       - Dark card (neutral-800 background)
       - Project name as heading
       - Description text (neutral-300)
       - Tech badges: small pills with tech names
       - Optional image placeholder area

    4. Hover effect with Framer Motion:
       - whileHover={{ scale: 1.02, y: -4 }}
       - Subtle shadow increase on hover
       - Transition duration ~0.2s

    5. Click handler prep:
       - onClick prop that will trigger modal (implement in next plan)
       - For now: console.log or empty function
       - cursor-pointer styling

    6. Framer Motion animations:
       - Staggered fade-in on mount
       - Follow TechStacks pattern

    Bento grid approach:
    ```
    grid-cols-1 md:grid-cols-2 lg:grid-cols-3
    featured cards: md:col-span-2
    ```
  </action>
  <verify>bun run type-check passes</verify>
  <done>ProjectsBento component renders cards with hover effects</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ProjectsBento into PortfolioPage</name>
  <files>apps/web/src/components/PortfolioPage.tsx</files>
  <action>
    1. Import ProjectsBento component
    2. Replace "{/* Projects goes here */}" comment with <ProjectsBento />
    3. Add section title "Proyectos" above component
    4. Follow same section structure as Timeline and TechStacks sections
  </action>
  <verify>bun run build succeeds</verify>
  <done>ProjectsBento integrated into PortfolioPage</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Projects bento grid with 4 cards and hover effects</what-built>
  <how-to-verify>
    1. Run: cd apps/web && bun run dev
    2. Visit: http://localhost:5173/portfolio
    3. Scroll to Projects section

    Bento layout:
    4. Desktop: Featured cards are wider (2 columns)
    5. Mobile: All cards stack vertically
    6. 4 project cards visible

    Card content:
    7. Each shows name, description
    8. Tech badges visible as small pills

    Hover effect:
    9. Hover over a card
    10. Verify: Card lifts slightly and scales up
    11. Verify: Smooth transition (~0.2s)

    Click:
    12. Click a card (should do nothing yet - modal in next plan)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run build succeeds
- [ ] bun run type-check passes
- [ ] Bento grid displays 4 cards
- [ ] Featured cards are larger on desktop
- [ ] Hover effects work (scale + lift)
- [ ] Tech badges display on cards
</verification>

<success_criteria>
- All tasks completed
- Projects data properly typed
- Bento grid layout works responsively
- Hover effects smooth and visible
- User approved visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-projects-bento/08-01-SUMMARY.md`
</output>
