---
wave: 1
autonomous: true
---

# Plan 10-01: Sidebar Structure & Search

## Objective

Create the sidebar component with fixed positioning on desktop and collapsible behavior on mobile. Add search input field (UI only).

**Requirements covered:**
- BLOG-02: Sidebar is fixed on desktop (doesn't scroll with content)
- BLOG-03: Sidebar is collapsible/hidden on mobile with toggle
- SIDE-01: Sidebar displays search input field (UI only, functionality deferred)

## Context

**Read before executing:**
- @apps/web/src/components/BlogPage.tsx (current placeholder sidebar)
- @apps/web/src/components/Header.tsx (mobile toggle pattern)

**Patterns to follow:**
- Mobile-first responsive with `md:` breakpoints
- State for toggle using `useState`
- Fixed positioning with `position: sticky` or `fixed`
- Tailwind v4 with @import syntax

## Tasks

### Task 1: Create BlogSidebar component with search

**Files:** `apps/web/src/components/BlogSidebar.tsx` (new)

**Implementation:**
```tsx
import { useState } from 'react'

interface BlogSidebarProps {
  className?: string
}

export function BlogSidebar({ className = '' }: BlogSidebarProps) {
  return (
    <div className={`space-y-6 ${className}`}>
      {/* Search - SIDE-01 */}
      <div>
        <h3 className="text-sm font-semibold text-white mb-3">Search</h3>
        <input
          type="text"
          placeholder="Search posts..."
          className="w-full px-3 py-2 bg-neutral-700 border border-neutral-600 rounded-lg text-white placeholder-neutral-400 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
        />
      </div>

      {/* Calendar placeholder - will be implemented in 10-02 */}
      <div>
        <h3 className="text-sm font-semibold text-white mb-3">Calendar</h3>
        <div className="bg-neutral-700 rounded-lg p-4 text-neutral-400 text-sm">
          Calendar widget
        </div>
      </div>

      {/* Tags placeholder - will be implemented in 10-02 */}
      <div>
        <h3 className="text-sm font-semibold text-white mb-3">Tags</h3>
        <div className="bg-neutral-700 rounded-lg p-4 text-neutral-400 text-sm">
          Tags cloud
        </div>
      </div>

      {/* Categories placeholder - will be implemented in 10-02 */}
      <div>
        <h3 className="text-sm font-semibold text-white mb-3">Categories</h3>
        <div className="bg-neutral-700 rounded-lg p-4 text-neutral-400 text-sm">
          Categories list
        </div>
      </div>
    </div>
  )
}
```

**Verification:**
- [ ] BlogSidebar.tsx exists
- [ ] Search input field renders
- [ ] Placeholder sections for calendar, tags, categories

### Task 2: Update BlogPage with fixed sidebar and mobile toggle

**Files:** `apps/web/src/components/BlogPage.tsx`

**Changes:**
1. Import useState and BlogSidebar
2. Add state for mobile sidebar visibility
3. Make sidebar fixed/sticky on desktop
4. Add toggle button for mobile
5. Conditionally show/hide sidebar on mobile

**Implementation pattern:**
```tsx
import { useState } from 'react'
import { BlogSidebar } from './BlogSidebar'

export function BlogPage() {
  const [sidebarOpen, setSidebarOpen] = useState(false)

  return (
    <div className="max-w-6xl mx-auto px-6 py-8">
      <div className="flex items-center justify-between mb-8">
        <h1 className="text-3xl font-bold text-white">Blog</h1>
        {/* Mobile toggle button */}
        <button
          className="md:hidden p-2 text-neutral-300 hover:text-white"
          onClick={() => setSidebarOpen(!sidebarOpen)}
          aria-label="Toggle sidebar"
        >
          <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>

      <div className="flex flex-col md:flex-row gap-8">
        {/* Sidebar - hidden on mobile unless toggled, sticky on desktop */}
        <aside className={`w-full md:w-64 shrink-0 ${sidebarOpen ? 'block' : 'hidden'} md:block`}>
          <div className="md:sticky md:top-24 bg-neutral-800 rounded-lg p-4">
            <BlogSidebar />
          </div>
        </aside>

        {/* Main content area */}
        <main className="flex-1">
          <div className="bg-neutral-800 rounded-lg p-6">
            <p className="text-neutral-400">Blog posts will appear here</p>
          </div>
        </main>
      </div>
    </div>
  )
}
```

**Verification:**
- [ ] Mobile: sidebar hidden by default
- [ ] Mobile: toggle button visible, toggles sidebar
- [ ] Desktop: sidebar always visible, toggle hidden
- [ ] Desktop: sidebar stays fixed (sticky) while content scrolls
- [ ] Search input visible in sidebar

## Success Criteria

- [ ] Search input renders (UI only, no functionality)
- [ ] Desktop: sidebar is sticky/fixed, doesn't scroll with content
- [ ] Mobile: sidebar hidden by default
- [ ] Mobile: toggle button reveals/hides sidebar

## Commit Format

```
feat(10-01): create BlogSidebar component with search input
feat(10-01): add mobile toggle and sticky sidebar to BlogPage
```
