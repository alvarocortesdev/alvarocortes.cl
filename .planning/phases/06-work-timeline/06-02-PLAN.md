---
phase: 06-work-timeline
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/src/components/Timeline.tsx
autonomous: false

must_haves:
  truths:
    - "Timeline scrolls horizontally on desktop (md: and above)"
    - "Timeline stacks vertically on mobile (below md:)"
    - "Horizontal scroll includes visual scroll affordance"
  artifacts:
    - path: "apps/web/src/components/Timeline.tsx"
      provides: "Responsive timeline with horizontal/vertical layouts"
      contains: "overflow-x"
  key_links: []
---

<objective>
Add responsive design to Timeline: horizontal scroll on desktop, vertical stack on mobile.

Purpose: Make timeline usable on all screen sizes with appropriate layout per device.
Output: Responsive Timeline that scrolls horizontally on desktop and stacks vertically on mobile.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@apps/web/src/components/Timeline.tsx
@apps/web/src/components/PortfolioPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add horizontal scroll for desktop</name>
  <files>apps/web/src/components/Timeline.tsx</files>
  <action>
    Update Timeline component for desktop horizontal scroll:

    1. Container styling:
       - overflow-x-auto for horizontal scrolling
       - Hide scrollbar or style it minimally (scrollbar-hide or webkit-scrollbar)
       - pb-4 for scroll padding

    2. Inner flex container:
       - md:flex-row (horizontal on desktop)
       - gap-4 or gap-6 between cards
       - min-w-max to prevent cards from shrinking

    3. Card sizing:
       - md:w-64 or md:w-72 (fixed width on desktop)
       - md:flex-shrink-0 (prevent shrinking)

    4. Optional: Add scroll affordance
       - Subtle gradient fade on right edge
       - Or small arrow/indicator showing scrollable

    Follow existing Tailwind patterns (mobile-first, md: breakpoints).
  </action>
  <verify>bun run type-check passes</verify>
  <done>Desktop layout scrolls horizontally</done>
</task>

<task type="auto">
  <name>Task 2: Add vertical stack for mobile</name>
  <files>apps/web/src/components/Timeline.tsx</files>
  <action>
    Ensure mobile layout stacks vertically:

    1. Default (mobile) flex container:
       - flex-col (stack vertically)
       - gap-4 between cards
       - Full width cards (no fixed width)

    2. Card styling for mobile:
       - w-full on mobile
       - Comfortable padding (p-4 or p-6)

    3. Optional timeline visual:
       - Vertical line connecting cards on mobile
       - Small dot/circle on left side of each card
       - Use before/after pseudo-elements or border-left

    Mobile-first approach: default styles are mobile, md: adds desktop overrides.
  </action>
  <verify>bun run build succeeds</verify>
  <done>Mobile layout stacks vertically with optional timeline visual</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive timeline layout</what-built>
  <how-to-verify>
    1. Run: cd apps/web && bun run dev
    2. Visit: http://localhost:5173/portfolio

    Desktop (wide screen):
    3. Verify: Timeline cards arranged horizontally
    4. Verify: Can scroll horizontally to see all entries
    5. Verify: Scroll affordance visible (fade/indicator)

    Mobile (resize browser to ~375px):
    6. Verify: Timeline cards stack vertically
    7. Verify: Each card takes full width
    8. Verify: Optional timeline visual connects cards

    Breakpoint test:
    9. Slowly resize browser width
    10. Verify: Clean transition at md: breakpoint (768px)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run build succeeds
- [ ] bun run type-check passes
- [ ] Desktop: horizontal scroll works
- [ ] Mobile: vertical stack works
- [ ] Breakpoint transition is clean
</verification>

<success_criteria>
- All tasks completed
- Horizontal scroll on desktop (768px+)
- Vertical stack on mobile (<768px)
- No layout breakage at any viewport size
- User approved responsive behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-timeline/06-02-SUMMARY.md`
</output>
