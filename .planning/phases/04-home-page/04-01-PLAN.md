---
phase: 04-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/Banner.tsx
  - apps/web/src/components/Bio.tsx
  - apps/web/src/components/HomePage.tsx
  - apps/web/src/App.tsx
autonomous: false

# Goal-backward verification
must_haves:
  truths:
    - "Banner displays photo placeholder, name, and job title"
    - "Bio section shows personal description text"
    - "Home page uses Layout component with header/footer"
  artifacts:
    - path: "apps/web/src/components/Banner.tsx"
      provides: "Hero banner with photo, name, job title"
      min_lines: 15
    - path: "apps/web/src/components/Bio.tsx"
      provides: "Personal description section"
      min_lines: 10
    - path: "apps/web/src/components/HomePage.tsx"
      provides: "Home page combining Banner and Bio"
      min_lines: 10
  key_links:
    - from: "HomePage.tsx"
      to: "Banner.tsx"
      via: "import and render"
      pattern: "import.*Banner"
    - from: "HomePage.tsx"
      to: "Bio.tsx"
      via: "import and render"
      pattern: "import.*Bio"
    - from: "App.tsx"
      to: "HomePage.tsx"
      via: "renders HomePage"
      pattern: "<HomePage"
---

<objective>
Create the Home page content with Banner and Bio sections.

Purpose: This is the main content visitors see - a professional first impression with photo, name, title, and personal description. The page uses the Layout component from Phase 3.
Output: Complete Home page with Banner and Bio sections, visually polished for desktop.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase provides Layout, Header, Footer:
@.planning/phases/03-layout-system/03-02-SUMMARY.md

# Current source files:
@apps/web/src/components/Layout.tsx
@apps/web/src/components/Header.tsx
@apps/web/src/App.tsx
@apps/web/src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Banner component</name>
  <files>apps/web/src/components/Banner.tsx</files>
  <action>
    Create Banner component as the hero section:

    **Content (per PROJECT.md and REQUIREMENTS):**
    - Photo: Circular placeholder (can be div with initials "AC" or placeholder image)
    - Name: "Alvaro Cortes" prominently displayed
    - Job Title: "Full Stack Developer" or similar professional title

    **Styling (Tailwind v4):**
    - Full-width section with generous vertical padding
    - Centered layout for desktop
    - Photo: Large circular element (w-32 h-32 or similar)
    - Name: Large, bold heading (text-4xl or text-5xl)
    - Title: Subtle secondary text (text-xl, text-neutral-400)
    - Dark theme matching existing components
    - Use max-w-6xl mx-auto px-6 pattern (established in Header)

    **Visual reference:** adhamdannaway.com style - clean, professional, minimal

    **Structure:**
    ```tsx
    export function Banner() {
      return (
        <section className="...">
          <div className="max-w-6xl mx-auto px-6 py-20">
            {/* Photo */}
            {/* Name */}
            {/* Job Title */}
          </div>
        </section>
      )
    }
    ```

    Keep it simple for v1.0 - no animations yet (can enhance later).
  </action>
  <verify>
    - File exists at apps/web/src/components/Banner.tsx
    - `bun run type-check` passes
    - Component exports Banner function
  </verify>
  <done>Banner component created with photo placeholder, name, and job title</done>
</task>

<task type="auto">
  <name>Task 2: Create Bio component</name>
  <files>apps/web/src/components/Bio.tsx</files>
  <action>
    Create Bio component for personal description:

    **Content:**
    - Personal introduction paragraph
    - Can use placeholder text for now:
      "Desarrollador Full Stack apasionado por crear experiencias web excepcionales.
       Especializado en React, TypeScript y arquitecturas modernas.
       Comprometido con el código limpio y las mejores prácticas de desarrollo."

    **Styling (Tailwind v4):**
    - Section with appropriate padding
    - Readable text width (max-w-3xl or prose-like)
    - Centered within max-w-6xl container
    - Text size appropriate for reading (text-lg)
    - Line height for readability (leading-relaxed)
    - Subtle text color (text-neutral-300)

    **Structure:**
    ```tsx
    export function Bio() {
      return (
        <section className="...">
          <div className="max-w-6xl mx-auto px-6 py-16">
            <div className="max-w-3xl mx-auto">
              {/* Bio text */}
            </div>
          </div>
        </section>
      )
    }
    ```
  </action>
  <verify>
    - File exists at apps/web/src/components/Bio.tsx
    - `bun run type-check` passes
    - Component exports Bio function
  </verify>
  <done>Bio component created with personal description text</done>
</task>

<task type="auto">
  <name>Task 3: Create HomePage component and update App.tsx</name>
  <files>apps/web/src/components/HomePage.tsx, apps/web/src/App.tsx</files>
  <action>
    **Create HomePage component:**

    Combines Banner and Bio within Layout context:

    ```tsx
    import { Banner } from './Banner'
    import { Bio } from './Bio'

    export function HomePage() {
      return (
        <>
          <Banner />
          <Bio />
        </>
      )
    }
    ```

    The Layout wrapper is applied in App.tsx, not here. HomePage just contains the page content.

    **Update App.tsx:**

    Replace placeholder content with HomePage:

    ```tsx
    import { Layout } from "./components/Layout"
    import { HomePage } from "./components/HomePage"

    export function App() {
      return (
        <Layout>
          <HomePage />
        </Layout>
      )
    }
    ```

    This satisfies HOME-03: Home page uses Layout component with header/footer.
  </action>
  <verify>
    - HomePage.tsx exists and imports Banner, Bio
    - App.tsx imports and uses HomePage
    - `bun run type-check` passes
    - `bun run build` succeeds
  </verify>
  <done>HomePage component created and integrated into App.tsx</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Home page with Banner (photo, name, title) and Bio (description) sections</what-built>
  <how-to-verify>
    1. Run: `cd apps/web && bun dev`
    2. Visit: http://localhost:5173
    3. Verify Banner section:
       - Photo placeholder visible (circle with "AC" or image)
       - Name "Alvaro Cortes" prominently displayed
       - Job title visible below name
    4. Verify Bio section:
       - Personal description text readable
       - Proper spacing between sections
    5. Verify Layout integration:
       - Header visible at top with logo/name and "Inicio" link
       - Content fades in smoothly (progressive loading)
       - Footer reveals when scrolling to bottom
    6. Overall visual impression:
       - Professional, clean appearance
       - Dark theme consistent throughout
       - Readable typography
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual/layout issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install` succeeds at repo root
- [ ] `bun run type-check` passes in apps/web
- [ ] `bun run build` succeeds
- [ ] Banner.tsx exists with photo, name, title
- [ ] Bio.tsx exists with description text
- [ ] HomePage.tsx combines Banner and Bio
- [ ] App.tsx uses HomePage within Layout
- [ ] Human verification passed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Banner displays photo placeholder, name, and job title
- Bio displays personal description
- Home page properly integrated with Layout
- Human approved visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/04-home-page/04-01-SUMMARY.md`
</output>
