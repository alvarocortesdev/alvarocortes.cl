---
phase: 05-portfolio-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/components/PortfolioPage.tsx
  - apps/web/src/components/Header.tsx
autonomous: false

must_haves:
  truths:
    - "Header shows 'Portfolio' link next to 'Inicio'"
    - "/portfolio route loads without errors"
    - "Portfolio page uses Layout component with header/footer"
  artifacts:
    - path: "apps/web/src/components/PortfolioPage.tsx"
      provides: "Portfolio page component"
      min_lines: 15
    - path: "apps/web/src/App.tsx"
      provides: "Router setup with Home and Portfolio routes"
      contains: "BrowserRouter"
    - path: "apps/web/src/components/Header.tsx"
      provides: "Navigation with Portfolio link"
      contains: "Portfolio"
  key_links:
    - from: "App.tsx"
      to: "PortfolioPage"
      via: "react-router Route"
      pattern: "Route.*portfolio"
    - from: "Header.tsx"
      to: "/portfolio"
      via: "Link component"
      pattern: "Link.*to.*portfolio"
    - from: "PortfolioPage.tsx"
      to: "Layout"
      via: "component wrapper"
      pattern: "<Layout>"
---

<objective>
Set up Portfolio page with client-side routing and navigation.

Purpose: Create the foundation for the Portfolio page with proper routing and navigation links in the header.
Output: Working /portfolio route with Layout, updated header navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/App.tsx
@apps/web/src/main.tsx
@apps/web/src/components/Header.tsx
@apps/web/src/components/Layout.tsx
@apps/web/src/components/HomePage.tsx
@apps/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom and set up routing</name>
  <files>apps/web/package.json, apps/web/src/main.tsx, apps/web/src/App.tsx</files>
  <action>
    1. Install react-router-dom: `bun add react-router-dom` in apps/web
    2. In main.tsx, wrap App with BrowserRouter from react-router-dom
    3. In App.tsx, replace direct HomePage render with Routes/Route setup:
       - Route path="/" element={<Layout><HomePage /></Layout>}
       - Route path="/portfolio" element={<Layout><PortfolioPage /></Layout>}
    4. Import PortfolioPage (will create in next task)

    Note: Keep Layout wrapping each route's content, not the Routes component (each page decides its own layout).
  </action>
  <verify>bun run type-check passes (may have import error until PortfolioPage exists)</verify>
  <done>Router installed, BrowserRouter wrapping App, Routes defined</done>
</task>

<task type="auto">
  <name>Task 2: Create PortfolioPage component</name>
  <files>apps/web/src/components/PortfolioPage.tsx</files>
  <action>
    Create PortfolioPage.tsx with:
    1. Basic page structure with sections for future content
    2. Similar styling to HomePage (dark theme, centered content)
    3. Section placeholders:
       - Timeline section (empty div with comment "Timeline goes here")
       - Tech Stacks section (empty div with comment "Tech Stacks goes here")
       - Projects section (empty div with comment "Projects goes here")
    4. Page title "Portfolio" at top

    Do NOT wrap in Layout - App.tsx handles that.
    Follow existing HomePage.tsx patterns for styling consistency.
  </action>
  <verify>bun run type-check passes, no TypeScript errors</verify>
  <done>PortfolioPage.tsx created with section placeholders</done>
</task>

<task type="auto">
  <name>Task 3: Update Header with Portfolio link</name>
  <files>apps/web/src/components/Header.tsx</files>
  <action>
    1. Import Link from react-router-dom
    2. Replace href="#" on "Inicio" with Link to="/"
    3. Add "Portfolio" link next to "Inicio":
       - Desktop nav: Link to="/portfolio" with same styling as Inicio
       - Mobile nav: Link to="/portfolio" in dropdown
    4. Add gap between nav links (gap-6 or similar)
    5. Close mobile menu when a link is clicked (setMenuOpen(false) in onClick)

    Keep existing responsive behavior - hamburger on mobile, links on desktop.
  </action>
  <verify>bun run build succeeds, no TypeScript errors</verify>
  <done>Header shows Inicio and Portfolio links, both desktop and mobile</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Portfolio page foundation with routing and navigation</what-built>
  <how-to-verify>
    1. Run: cd apps/web && bun run dev
    2. Visit: http://localhost:5173/
    3. Verify: Header shows "Inicio" and "Portfolio" links
    4. Click "Portfolio" link
    5. Verify: URL changes to /portfolio, PortfolioPage loads
    6. Verify: Header and footer work on Portfolio page
    7. Click "Inicio" link
    8. Verify: Returns to homepage
    9. Test mobile: Resize to mobile width
    10. Verify: Hamburger menu shows both links
    11. Verify: Clicking link closes menu and navigates
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run build succeeds
- [ ] bun run type-check passes
- [ ] Both / and /portfolio routes work
- [ ] Header shows both navigation links
- [ ] Mobile menu works correctly
</verification>

<success_criteria>
- All tasks completed
- Navigation works between Home and Portfolio pages
- Layout component works on both pages
- Mobile navigation includes Portfolio link
- User approved visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/05-portfolio-foundation/05-01-SUMMARY.md`
</output>
