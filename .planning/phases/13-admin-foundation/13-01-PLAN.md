---
phase: 13-admin-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/tsconfig.json
  - apps/admin/vite.config.ts
  - apps/admin/index.html
  - apps/admin/src/main.tsx
  - apps/admin/src/App.tsx
  - apps/admin/src/styles/main.css
autonomous: true

must_haves:
  truths:
    - "Admin app runs at localhost:5174"
    - "Turborepo builds admin app with turbo build"
    - "Admin app has Tailwind CSS working"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "Workspace definition with @alvarocortes/admin name"
      contains: "@alvarocortes/admin"
    - path: "apps/admin/src/App.tsx"
      provides: "Admin app shell component"
      min_lines: 10
  key_links:
    - from: "apps/admin/package.json"
      to: "turbo.json"
      via: "apps/* workspace glob auto-discovery"
---

<objective>
Create the admin app workspace as a Vite + React + Tailwind application following existing web app patterns.

Purpose: Establish the apps/admin workspace that Turborepo will auto-discover and build alongside the existing web app.
Output: Functional admin app that runs locally and builds via Turborepo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-foundation/13-RESEARCH.md

Existing patterns to follow:
@apps/web/package.json
@apps/web/vite.config.ts
@apps/web/tsconfig.json
@apps/web/src/main.tsx
@apps/web/src/App.tsx
@apps/web/src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin package.json</name>
  <files>apps/admin/package.json</files>
  <action>
Create apps/admin/package.json following the web app pattern:
- name: "@alvarocortes/admin"
- type: "module"
- scripts: dev, build, preview, lint, type-check
- dependencies: react, react-dom, react-router-dom, @supabase/supabase-js, @alvarocortes/shared
- devDependencies: @types/react, @types/react-dom, @vitejs/plugin-react, @tailwindcss/vite, tailwindcss, vite, vite-tsconfig-paths, typescript

Use same versions as web app for consistency. Dev server should run on port 5174 (vite default for second app).
  </action>
  <verify>cat apps/admin/package.json shows valid JSON with correct name</verify>
  <done>Package.json exists with @alvarocortes/admin name and all dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create admin Vite and TypeScript config</name>
  <files>apps/admin/vite.config.ts, apps/admin/tsconfig.json</files>
  <action>
Create vite.config.ts:
- Import react plugin, tailwindcss vite plugin, tsconfigPaths
- Set server.port to 5174
- Configure @ alias to src directory

Create tsconfig.json:
- Extend from root or use same settings as web
- Set paths alias "@/*" -> ["./src/*"]
- Include src/**/*.ts, src/**/*.tsx
  </action>
  <verify>Files exist and have valid syntax</verify>
  <done>Vite and TypeScript configs ready</done>
</task>

<task type="auto">
  <name>Task 3: Create admin app entry files</name>
  <files>apps/admin/index.html, apps/admin/src/main.tsx, apps/admin/src/App.tsx, apps/admin/src/styles/main.css</files>
  <action>
Create index.html:
- Standard Vite HTML template
- Title: "Admin | Alvaro Cortes"
- Script src="/src/main.tsx" type="module"

Create src/main.tsx:
- Import React, ReactDOM
- Import main.css
- Import BrowserRouter from react-router-dom
- Render App wrapped in BrowserRouter

Create src/App.tsx:
- Simple placeholder component
- Display "Admin Panel" heading
- Display "Phase 14 will add authentication" as placeholder text
- Use Tailwind classes: bg-neutral-900 min-h-screen text-white

Create src/styles/main.css:
- @import "tailwindcss" (Tailwind v4 syntax)
  </action>
  <verify>All files exist, no syntax errors</verify>
  <done>Admin app has all entry point files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install` at root succeeds (installs admin deps)
- [ ] `bun run dev --filter=@alvarocortes/admin` starts dev server on port 5174
- [ ] `turbo build` builds both web and admin apps
- [ ] Browser at http://localhost:5174 shows "Admin Panel" heading
</verification>

<success_criteria>

- All tasks completed
- Admin workspace auto-discovered by Turborepo
- Dev server runs without errors
- Tailwind CSS styles render correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/13-admin-foundation/13-01-SUMMARY.md`
</output>
